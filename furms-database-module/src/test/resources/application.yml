spring:
  datasource:
    url: jdbc:h2:mem:furms;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_UPPER=TRUE
    username: sa
    password: sa
    driver-class-name: org.h2.Driver
  flyway:
    enabled: true